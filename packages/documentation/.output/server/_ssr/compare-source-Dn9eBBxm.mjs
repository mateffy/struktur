import { X as loader, Y as lucideIconsPlugin, _ as compare } from "./staticFunctionMiddleware-BO_LZi6-.mjs";
const compareSource = loader({
  source: compare.toFumadocsSource(),
  baseUrl: "/compare",
  plugins: [lucideIconsPlugin()]
});
export {
  compareSource as c
};
